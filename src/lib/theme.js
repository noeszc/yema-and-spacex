import { theme as defaultTheme } from '@chakra-ui/core';

const fonts = {
  heading: `'Barlow', sans-serif`,
  body: `'Barlow', sans-serif`,
};

const theme = Object.assign(defaultTheme, { fonts });

export default theme;
